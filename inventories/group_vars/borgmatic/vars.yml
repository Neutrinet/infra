borgmatic_repositories:
  - host: storage-01.ovh.neutri.net
    path: /media/data/backups
    user: backup-borg
borgmatic_encryption_passphrase: "{{ vault_borgmatic_encryption_passphrase }}"
borgmatic_encryption_mode: keyfile
