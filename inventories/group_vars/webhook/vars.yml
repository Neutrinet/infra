webhook_config:
  - name: github-pull-labriqueinter-net
    type: github
    become_user: labriqueinter-net
    script: pull.sh
    working_dir: /var/www/labriqueinter-net/dist
    token: "{{ vault_webhook_github_pull_token }}"

webhook_scripts:
  - webhook/scripts/pull.sh
