uptime_kuma_domain: uptime.neutrinet.be
uptime_kuma_owner: "{{ uptime_kuma_domain | replace('.', '-') }}"
uptime_kuma_version: 1.23.2

# We first need to create the admin user
# Then, go to Settings > API Keys and create a new token
uptime_kuma_api_token: "{{ vault_uptime_kuma_api_token }}"
