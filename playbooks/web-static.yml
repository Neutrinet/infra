---
# Déploiement des sites web statiques
- hosts: web_static
  become: yes

  pre_tasks:
    - name: Update cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

  roles:
    - web-static