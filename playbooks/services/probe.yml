---
- name: Déploiement des probes pour le monitoring
  hosts: probe
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  roles:
    - probe
    - blackbox_exporter
    - nagios_openvpn
