---
- name: Déploiement de Gitlab Runner
  hosts: gitlab_runner
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  roles:
    - gitlab_runner
