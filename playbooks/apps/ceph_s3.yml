---
- name: Déploiement du cluster Ceph S3
  hosts: ceph_s3
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  roles:
    - ceph_s3
