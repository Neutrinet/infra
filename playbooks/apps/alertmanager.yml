---
# Déploiement d'Alertmanager
- hosts: alertmanager
  become: true

  pre_tasks:
    - name: Update cache
      apt:
        update_cache: true
        cache_valid_time: 3600

  roles:
    - alertmanager
