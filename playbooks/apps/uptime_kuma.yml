---
- name: Déploiement de Uptime-Kuma
  hosts: uptime_kuma
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  roles:
    - uptime_kuma
