---
# Déploiement du cluster Patroni pour PostgreSQL
- hosts: postgres_patroni
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      apt:
        update_cache: true

  roles:
    - postgres_patroni
