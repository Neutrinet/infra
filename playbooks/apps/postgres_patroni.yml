---
# Déploiement du cluster Patroni pour PostgreSQL
- hosts: postgres_patroni
  become: true

  pre_tasks:
    - name: Update cache
      apt:
        update_cache: true
        cache_valid_time: 3600

  roles:
    - postgres_patroni
