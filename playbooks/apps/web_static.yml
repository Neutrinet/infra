---
- name: Déploiement des sites web statiques
  hosts: web_static
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  roles:
    - web_static
