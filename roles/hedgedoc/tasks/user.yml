- name: Création du dossier /var/www
  ansible.builtin.file:
    path: /var/www
    owner: root
    group: root
    mode: "u=rwx,go=rx"
    state: directory

- name: Création de l'utilisateur {{ hedgedoc_owner }}
  ansible.builtin.user:
    name: "{{ hedgedoc_owner }}"
    home: /var/www/{{ hedgedoc_owner }}
    shell: /sbin/nologin
    system: true
    state: present
