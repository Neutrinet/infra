---
- name: Copie de la config de journald
  ansible.builtin.template:
    src: systemd/journald.conf.j2
    dest: /etc/systemd/journald.conf
    owner: root
    group: root
    mode: "u=rw,go=r"
  notify: restart systemd-journald
