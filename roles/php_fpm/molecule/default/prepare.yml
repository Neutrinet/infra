---
- name: Prepare
  hosts: all
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  tasks:
    - name: Installation de GPG
      ansible.builtin.package:
        name: gpg
        state: present
