{{ ansible_managed | comment }}

<<: !include /etc/borgmatic/template.yml

location:
  source_directories:
    - "/etc/routinator"

storage:
  archive_name_format: routinator_{hostname}-{now}

retention:
  prefix: routinator_

consistency:
  prefix: routinator_
