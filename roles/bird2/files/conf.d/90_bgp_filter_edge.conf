filter edge_import
{
    if bgp_large_community ~ [(my_asn, 0, 20)] then {
        accept;
    }
    else reject;
}

filter edge_export
{
    print "haaaaaaa aa ", net;
    if source = RTS_DEVICE then {
        bgp_large_community.add((my_asn, 0, 20));
        accept;
    }
    else reject;
}
