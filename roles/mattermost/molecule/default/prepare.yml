---
- name: Prepare
  hosts: all
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      apt:
        update_cache: true

    - name: Installation de Git
      package:
        name: git
        state: present

  roles:
    - telegraf
    - borgmatic
    - postgres_standalone
