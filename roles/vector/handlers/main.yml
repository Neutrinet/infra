- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Restart vector
  ansible.builtin.service:
    name: vector
    state: restarted

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
