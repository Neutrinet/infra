---
- name: Prepare
  hosts: all
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      apt:
        update_cache: true

    - name: Installation des ACL
      package:
        name: acl
        state: present

  roles:
    - telegraf
    - postgres_standalone
