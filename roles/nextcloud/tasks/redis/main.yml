- name: Installation de Redis
  import_tasks: install.yml
  vars:
    redis_port: "{{ nextcloud_redis_port }}"
    redis_socket: "{{ nextcloud_redis_socket }}"
    redis_socket_mode: "ug=rwx,o="
  when: nextcloud_redis_install_enabled
  tags: redis_install

- name: Ajout de l'utilisateur {{ web_app_owner }} au groupe redis
  user:
    name: "{{ web_app_owner }}"
    state: present
    groups:
      - redis
    append: true
  notify:
    - reload php-fpm
