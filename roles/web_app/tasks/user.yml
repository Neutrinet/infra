- name: Création de l'utilisateur {{ web_app_owner }}
  ansible.builtin.user:
    name: "{{ web_app_owner }}"
    home: "{{ web_app_home_dir }}"
    shell: /sbin/nologin
    system: true
    state: present
