{{ ansible_managed | comment }}

<<: !include /etc/borgmatic.d/common.yml

source_directories: !retain
  - "/etc/caddy/conf.d/{{ dokuwiki_owner }}.conf"
  - "{{ web_app_dist_dir }}"
  - "{{ php_fpm_pool_dir }}/{{ dokuwiki_owner }}.conf"

archive_name_format: dokuwiki-{{ dokuwiki_owner }}_{hostname}-{now}
