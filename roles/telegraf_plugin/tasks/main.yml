- name: Copie de la configuration Telegraf
  template:
    src: "{{ telegraf_plugin_template }}"
    dest: /etc/telegraf/telegraf.d/{{ telegraf_plugin_name }}.conf
    owner: telegraf
    group: telegraf
    mode: "u=rw,g=r,o="
    validate: telegraf --config %s --test
  notify: restart telegraf

- name: Ajout de groupes à l'utilisateur telegraf
  user:
    name: telegraf
    groups: "{{ telegraf_append_groups }}"
    append: true
  notify: restart telegraf
  when: telegraf_append_groups | length > 0
