- name: Récupération de la liste des dossiers d'installation
  ansible.builtin.find:
    path: /opt/x509-certificate-exporter
    file_type: directory
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
  register: _x509_certificate_exporter_installed_dirs

- name: Récupération de la liste des archives
  ansible.builtin.find:
    path: /usr/local/src/x509-certificate-exporter
    file_type: file
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+\.tar\.gz$'
  register: _x509_certificate_exporter_archives

- name: Récupération de la plus ancienne version et de la version courante
  ansible.builtin.set_fact:
    x509_certificate_exporter_oldest_archive: "{{ x509_certificate_exporter_sorted_archives | first }}"
    x509_certificate_exporter_oldest_install_dir: "{{ x509_certificate_exporter_sorted_install_dirs | first }}"
    x509_certificate_exporter_oldest_version: "{{ x509_certificate_exporter_sorted_install_dirs | first | basename }}"
  vars:
    x509_certificate_exporter_sorted_archives: |-
      {{ _x509_certificate_exporter_archives.files
        | map(attribute='path')
        | community.general.version_sort }}
    x509_certificate_exporter_sorted_install_dirs: |-
      {{ _x509_certificate_exporter_installed_dirs.files
        | map(attribute='path')
        | community.general.version_sort }}

- name: Suppression de la plus ancienne version (v{{ x509_certificate_exporter_oldest_version }})
  ansible.builtin.file:
    state: absent
    path: "{{ x509_certificate_exporter_oldest_install_dir }}"
  when: _x509_certificate_exporter_installed_dirs.files | length > 5

- name: Suppression de la plus ancienne archive
  ansible.builtin.file:
    state: absent
    path: "{{ x509_certificate_exporter_oldest_archive }}"
  when: _x509_certificate_exporter_archives.files | length > 5
