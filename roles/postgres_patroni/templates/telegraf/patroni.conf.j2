{{ ansible_managed | comment }}

[[inputs.prometheus]]
  ## Scrape metrics directly from Etcd
  urls = ["http://localhost:8008/metrics"]
  metric_version = 2

[[inputs.postgresql]]
  address = "user={{ postgresql_monitoring_username }} dbname=postgres"
  ignored_databases = ["template0", "template1"]
