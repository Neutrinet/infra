- name: Récupération de la liste des dossiers d'installation
  ansible.builtin.find:
    path: /opt/blackbox-exporter
    file_type: directory
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
  register: _blackbox_exporter_installed_dirs

- name: Récupération de la liste des archives
  ansible.builtin.find:
    path: /usr/local/src/blackbox-exporter
    file_type: file
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+\.tar\.gz$'
  register: _blackbox_exporter_archives

- name: Récupération de la plus ancienne version et de la version courante
  ansible.builtin.set_fact:
    blackbox_exporter_oldest_archive: "{{ blackbox_exporter_sorted_archives | first }}"
    blackbox_exporter_oldest_install_dir: "{{ blackbox_exporter_sorted_install_dirs | first }}"
    blackbox_exporter_oldest_version: "{{ blackbox_exporter_sorted_install_dirs | first | basename }}"
  vars:
    blackbox_exporter_sorted_archives: |-
      {{ _blackbox_exporter_archives.files
        | map(attribute='path')
        | community.general.version_sort }}
    blackbox_exporter_sorted_install_dirs: |-
      {{ _blackbox_exporter_installed_dirs.files
        | map(attribute='path')
        | community.general.version_sort }}

- name: Suppression de la plus ancienne version (v{{ blackbox_exporter_oldest_version }})
  ansible.builtin.file:
    state: absent
    path: "{{ blackbox_exporter_oldest_install_dir }}"
  when: _blackbox_exporter_installed_dirs.files | length > 5

- name: Suppression de la plus ancienne archive
  ansible.builtin.file:
    state: absent
    path: "{{ blackbox_exporter_oldest_archive }}"
  when: _blackbox_exporter_archives.files | length > 5
