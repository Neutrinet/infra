---
- name: Prepare
  hosts: all
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

    - name: Installation de Git
      ansible.builtin.package:
        name: git
        state: present

  roles:
    - telegraf
