- name: Récupération de la liste des dossiers d'installation
  ansible.builtin.find:
    path: /opt/collectd-exporter
    file_type: directory
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
  register: _collectd_exporter_installed_dirs

- name: Récupération de la liste des archives
  ansible.builtin.find:
    path: /usr/local/src/collectd-exporter
    file_type: file
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+\.tar\.gz$'
  register: _collectd_exporter_archives

- name: Récupération de la plus ancienne version et de la version courante
  ansible.builtin.set_fact:
    collectd_exporter_oldest_archive: "{{ collectd_exporter_sorted_archives | first }}"
    collectd_exporter_oldest_install_dir: "{{ collectd_exporter_sorted_install_dirs | first }}"
    collectd_exporter_oldest_version: "{{ collectd_exporter_sorted_install_dirs | first | basename }}"
  vars:
    collectd_exporter_sorted_archives: |-
      {{ _collectd_exporter_archives.files
        | map(attribute='path')
        | community.general.version_sort }}
    collectd_exporter_sorted_install_dirs: |-
      {{ _collectd_exporter_installed_dirs.files
        | map(attribute='path')
        | community.general.version_sort }}

- name: Suppression de la plus ancienne version (v{{ collectd_exporter_oldest_version }})
  ansible.builtin.file:
    state: absent
    path: "{{ collectd_exporter_oldest_install_dir }}"
  when: _collectd_exporter_installed_dirs.files | length > 5

- name: Suppression de la plus ancienne archive
  ansible.builtin.file:
    state: absent
    path: "{{ collectd_exporter_oldest_archive }}"
  when: _collectd_exporter_archives.files | length > 5
