---
- name: Installation de GPG
  package:
    name:
      - gpg
      - gpg-agent
      - dirmngr
    state: present

- name: Ajout de la clé GPG de ceph proxmox dans APT
  apt_key:
    url: "{{ ceph_proxmox_gpg_key_url }}"
    state: present

- name: Ajout du dépôt APT ceph proxmox
  apt_repository:
    repo: "deb http://download.proxmox.com/debian/ceph-{{ ceph_proxmox_version }} {{ ansible_distribution_release }} main"
    filename: proxmox-ceph
    state: present
