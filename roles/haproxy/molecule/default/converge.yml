---
- name: Converge
  hosts: all
  become: true

  tasks:
    - name: Update cache
      apt:
        update_cache: true
        cache_valid_time: 3600
      when: ansible_facts['os_family'] == 'Debian'
    - name: "Include haproxy"
      include_role:
        name: "haproxy"
