- name: Création du dossier /var/www
  ansible.builtin.file:
    path: /var/www
    owner: root
    group: root
    mode: "u=rwx,go=rx"
    state: directory

- name: Création de l'utilisateur {{ mobilizon_owner }}
  ansible.builtin.user:
    name: "{{ mobilizon_owner }}"
    home: /var/www/{{ mobilizon_owner }}
    shell: /sbin/nologin
    system: true
    state: present