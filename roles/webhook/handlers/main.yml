- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart webhook
  ansible.builtin.service:
    name: webhook
    state: restarted

- name: Reload webhook
  ansible.builtin.service:
    name: webhook
    state: reloaded
