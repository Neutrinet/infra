{{ ansible_managed | comment }}

<<: !include /etc/borgmatic/template.yml

location:
  source_directories:
    - "/etc/caddy/conf.d/{{ web_app_owner }}.conf"
    - "{{ web_app_dist_dir }}"
    - "{{ php_fpm_pool_dir }}/{{ web_app_owner }}.conf"

storage:
  archive_name_format: grav-{{ web_app_owner }}_{hostname}-{now}

retention:
  prefix: grav-{{ web_app_owner }}_

consistency:
  prefix: grav-{{ web_app_owner }}_
