---
- name: Prepare
  hosts: all
  become: true

  pre_tasks:
    - name: Mise à jour du cache APT
      ansible.builtin.apt:
        update_cache: true

  tasks:
    - name: Installation de GnuPG
      ansible.builtin.package:
        name: gnupg
        state: present
