- name: Récupération de la liste des dossiers d'installation
  ansible.builtin.find:
    path: /opt/node-exporter
    file_type: directory
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
  register: _node_exporter_installed_dirs

- name: Récupération de la liste des archives
  ansible.builtin.find:
    path: /usr/local/src/node-exporter
    file_type: file
    use_regex: true
    recurse: false
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+\.tar\.gz$'
  register: _node_exporter_archives

- name: Récupération de la plus ancienne version et de la version courante
  ansible.builtin.set_fact:
    node_exporter_oldest_archive: "{{ node_exporter_sorted_archives | first }}"
    node_exporter_oldest_install_dir: "{{ node_exporter_sorted_install_dirs | first }}"
    node_exporter_oldest_version: "{{ node_exporter_sorted_install_dirs | first | basename }}"
  vars:
    node_exporter_sorted_archives: |-
      {{ _node_exporter_archives.files
        | map(attribute='path')
        | community.general.version_sort }}
    node_exporter_sorted_install_dirs: |-
      {{ _node_exporter_installed_dirs.files
        | map(attribute='path')
        | community.general.version_sort }}

- name: Suppression de la plus ancienne version (v{{ node_exporter_oldest_version }})
  ansible.builtin.file:
    state: absent
    path: "{{ node_exporter_oldest_install_dir }}"
  when: _node_exporter_installed_dirs.files | length > 5

- name: Suppression de la plus ancienne archive
  ansible.builtin.file:
    state: absent
    path: "{{ node_exporter_oldest_archive }}"
  when: _node_exporter_archives.files | length > 5
